namespace Domain.Utilities;

public static class Roles
{
    public const string Admin = nameof(Admin);
    public const string Teacher = nameof(Teacher);
    public const string AcademicManager = nameof(AcademicManager);
}
